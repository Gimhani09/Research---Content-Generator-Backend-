# ============================================================
# Hugging Face Spaces deployment requirements
# NO local ML models — uses Gemini API for content generation
# ============================================================

# Core Web Framework
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
jinja2==3.1.2

# Gemini API (content generation + polishing)
google-generativeai==0.3.2
# Pin google core deps to versions compatible with google-generativeai==0.3.2
google-api-core>=1.34.0,<2.20.0
google-auth>=2.15.0,<3.0.0
googleapis-common-protos>=1.56.4,<2.0.0

# Poster rendering
Pillow==10.1.0
playwright==1.40.0

# Image background (Stability AI API calls only — no local model)
requests==2.31.0
httpx==0.25.2

# Utilities
python-dotenv==1.0.0
tqdm==4.66.1
numpy==1.24.3
